import 'dart:ffi';

import 'package:flutter/material.dart';

class AddWork extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Add new Worktime")),
    );
  }
}